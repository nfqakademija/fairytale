language: php

php:
    - 5.4
    - 5.5
    - 5.6

before_script:
    - composer install -n --prefer-source

script:
    - bin/phpspec run -v -fpretty
# Gotta install webserver on travis worker before running behat
#    - php bin/behat @NfqFairytaleApiBundle -v
